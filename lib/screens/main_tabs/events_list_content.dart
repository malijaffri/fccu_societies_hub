import 'package:flutter/material.dart';

class EventsListContent extends StatelessWidget {
  const EventsListContent({super.key});

  @override
  Widget build(BuildContext context) => Center(child: Text('Events Screen Placeholder'));
}
